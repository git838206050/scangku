后台：
1、主左导航栏（backstage/main/left.html）使用 本地地址栏URL获取并根据指定关键词实现展开导航组功能。所以添加导航时请自行观察适合的关键词进行添加。
2、每个html文件 第一个导入的文件均为 init.js文件（加载页面时提示加载中）
	并在最后导入 main.js文件。否则会因为左导航栏和上面的快捷跳转按钮加载太慢而导致位置错乱。
3、由于使用到jQuery 的 load 加载左导航页面 大部分主流浏览器出现禁止跨域问题，所以请放到服务器中运行。